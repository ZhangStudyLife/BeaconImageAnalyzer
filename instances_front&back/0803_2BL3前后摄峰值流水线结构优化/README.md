# 2BL3前后摄峰值流水线结构优化实例

该实例在第一轮等价优化基础上继续压缩信标峰值流水线，不修改任何识别阈值、分类参数、车灯规则、地平线模型或时序输出规则。

- 使用带4像素镜像边界的纵向列和，消除逐像素边界分支。
- 不再计算从未参与局部极大值判定的首尾响应行和最右无效响应列。
- 先执行响应、灰度和Top16下界检查，再访问地平线。
- Top16已满时，提前跳过不可能进入结果集的低响应点。
- 局部极大值判断保持原有8邻域和相同的严格小于语义。
- 算法Build ID为`0x20260908`。

在上位机中点击“加载实例”，选择本目录即可逐帧查看实际算法输出。
