# 2BL3前后摄二值前景位图调度实例

该实例基于扫描线连通域版本，优化二值连通域的外层调度，不修改阈值、8连通语义、候选特征、地平线、时序或输出规则。

- 二值化时同步生成逐行前景位图。
- 信标连通域补充和灰度车灯补检只遍历位图中的前景像素，不再逐像素扫描整幅二值图。
- 连通域内部仍由扫描线洪泛消费；已被同一连通域访问的位图位置通过二值状态检查跳过。
- 位图与稀疏积分响应缓存复用同一阶段性内存，不增加常驻大缓冲区。
- 前景位图只承担遍历调度，不改变任何候选的数学测量。
- 算法Build ID为`0x20260935`。
