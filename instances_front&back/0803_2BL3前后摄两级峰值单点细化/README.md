# 2BL3前后摄两级峰值单点细化实例

该实例修正第一版两级搜索的重复计算问题，仍不加入场景位置或视频专用参数。

- 原始灰度局部峰必须不低于8邻域，且至少严格高于一个邻点，排除整片等灰平台。
- 每个灰度峰只在3×3邻域选择一个最高精确盒响应位置进入完整极大值判定。
- 公共积分峰值检查改为内联，暗场稀疏路径不承担额外函数调用。
- 最终仍执行原有盒响应、Top16、地平线、车灯屏蔽和灰度特征分类。
- 算法Build ID为`0x20260914`。

该实例允许少量输出变化，必须通过完整视频与人工标注回归后才能采用。
