# 2BL3前后摄地平线计算域裁剪实例

该实例利用现有地平线的几何有效域缩小信标峰值计算范围，不修改地平线模型、容差或信标判定规则。

- 地平线有效且所有搜索列有效时，计算所有列中最早允许出现候选的整数行。
- 峰值响应从该行所需的上一邻域行开始生成，地平线上方不可能参与任何判定的响应行不再计算。
- 稀疏积分路径同步跳过不可能成为候选的顶部行。
- 地平线无效或任一搜索列无效时自动退化为原完整搜索范围。
- 保留原2像素地平线容差和逐列最终检查，输出语义不变。
- 算法Build ID为`0x20260916`。

在上位机中点击“加载实例”，选择本目录即可逐帧查看实际算法输出。
