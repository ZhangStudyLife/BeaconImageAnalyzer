# 2BL3前后摄四行响应缓存实例

该实例基于16位模积分图版本，将稀疏路径的整帧响应缓存重构为4行环形缓存，不修改峰值响应或识别规则。

- 峰值扫描按Y递增，单个中心点只访问`y-1`、`y`、`y+1`三行响应。
- 使用4个环形槽保存当前工作集，新的远端行只覆盖已经不会再次访问的旧行。
- 每行保留独立有效位图，确保同一响应仍只计算一次。
- 响应公式、局部极大值比较顺序、Top16顺序和最终输出保持不变。
- 环形响应缓存与二值前景位图继续复用同一阶段性工作区。
- 固定内存，无新增参数或场景判断。
- 算法Build ID为`0x20260938`。
