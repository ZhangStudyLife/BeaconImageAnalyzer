# 2BL3前后摄CM4位图乘法压缩实例

该实例基于CM4双半字模积分版本，只优化CM4二值比较结果到前景位图的压缩方式，不修改阈值或遍历规则。

- `USUB8+SEL`仍生成4个独立的全零/全一字节。
- 利用字节低位乘法权重，将4个字节的最低位一次压缩为连续4位。
- 等价公式为`((selected & 0x01010101) * 0x01020408) >> 24`。
- 替代原有多组移位、掩码和按位或，降低每4像素的指令数。
- 标量桌面路径保持不变，DSP仿真用于逐帧验证。
- 无新增参数、位置规则或内存缓冲。
- 算法Build ID为`0x20260944`。
