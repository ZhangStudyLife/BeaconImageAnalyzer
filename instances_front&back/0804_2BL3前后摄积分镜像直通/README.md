# 2BL3前后摄积分镜像直通实例

该实例基于二值前景位图调度版本，只优化稀疏峰值路径的镜像积分图构建，不修改任何检测公式或参数。

- 积分图左右各4像素的镜像边界显式处理。
- 188个图像内部像素直接顺序读取，不再对每个像素访问镜像索引表。
- 上下边界仍使用完全相同的镜像坐标公式。
- 积分值、3×3和9×9盒和及最终峰值响应保持逐点一致。
- 删除不再需要的X/Y镜像映射表，减少常驻状态。
- 算法Build ID为`0x20260936`。
