# 2BL3前后摄稀疏响应缓存验证实例

该实例在稀疏积分峰值路径基础上缓存已经计算过的响应，使相邻亮像素共享中心及8邻域结果，不修改识别公式或参数。

- 每个图像坐标的积分响应在一帧内最多计算一次。
- 使用位图记录缓存有效性，避免清空完整响应数组。
- 中等亮度场景也可选择稀疏路径；路径选择只影响运算量，不影响输出。
- 稠密路径、局部特征、车灯、地平线和时序逻辑保持不变。
- 算法Build ID为`0x20260912`。

在上位机中点击“加载实例”，选择本目录即可逐帧查看实际算法输出。
