# 0728 灰度点光源信标实例

该实例是自包含快照。加载时只会编译本目录中的代码：

- `algorithm/Image`：完整图像处理与参数表。
- `algorithm/host`：上位机实例适配层及兼容头文件。

不会读取或编译外部 `CYT2BL3_Image` 仓库中的源文件。

信标候选、位置和面积连续性选择、两帧预测，以及车灯检测代码都在该实例目录中。动态库输出到本目录的 `build` 文件夹。
