# 2BL3前后摄局部灰度特征结构优化实例

该实例在峰值流水线结构优化基础上，继续优化所有信标候选共用的局部灰度特征计算，不修改任何识别阈值和分类规则。

- 15×15外接方框改为只遍历实际使用的半径7圆形区域。
- 首次读取图像时缓存半径6核心像素，第二阶段不再重复访问原图和执行镜像坐标计算。
- 灰度环直方图只在实际最小值至最大值之间扫描，并在一次累计中同时得到中位数的两个秩。
- 内外灰度、径向衰减、集中度、形状、质心和面积公式保持不变。
- 算法Build ID为`0x20260909`。

在上位机中点击“加载实例”，选择本目录即可逐帧查看实际算法输出。
