# 2BL3前后摄亮像素位图峰值扫描实例

该实例基于扫描线连通域版本，只优化稀疏场景的峰值搜索遍历，不修改阈值、峰值响应、局部极大值、候选分类、地平线或时序规则。

- 计算全图平均灰度和亮像素数量时，同时生成灰度不低于60的逐行位图。
- 稀疏积分路径按位图中的置位像素升序遍历，不再逐个访问所有低灰度像素。
- 每个亮像素仍使用原积分响应、8邻域响应、Top16和车灯屏蔽判断。
- 稠密场景仍使用原有CM4 DSP双盒滤波流水线。
- 固定内存，无动态分配、视频位置规则或新增参数。
- 算法Build ID为`0x20260934`。
