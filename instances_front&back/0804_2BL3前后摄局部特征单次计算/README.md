# 2BL3前后摄局部特征单次计算实例

该实例以稀疏响应缓存版本为基线，将峰值初筛已经计算出的局部灰度特征随候选保存，最终可靠性筛选直接复用，不修改识别公式或参数。

- 同一个峰值候选的15×15局部灰度特征一帧内只计算一次。
- 连通域补充候选没有可复用特征时，仍按原流程惰性计算。
- 候选判定、最终可靠性公式、阈值、面积排序和时序逻辑保持不变。
- 稠密路径、局部特征、车灯、地平线和时序逻辑保持不变。
- 算法Build ID为`0x20260920`。

在上位机中点击“加载实例”，选择本目录即可逐帧查看实际算法输出。
