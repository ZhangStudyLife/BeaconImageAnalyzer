# 2BL3前后摄连通域坐标打包验证

该实例基于`0x20260930`，将连通域队列的X/Y字节数组打包为单个16位坐标，不修改识别规则和参数。

验证结果：

- 四段通用视频共11119帧，与`0x20260930`的B0/B1、CAR0和时序输出零差异。
- 强制执行CM4 DSP仿真路径时，与标量路径零差异。
- MinGW严格编译通过。
- PC整帧平均耗时相对`0x20260930`增加约1.78%，未观察到性能收益。

结论：该改动只减少队列源码层面的读写次数，但不足以成为主要优化方向；暂不作为后续实例基线，也不移植板端。
