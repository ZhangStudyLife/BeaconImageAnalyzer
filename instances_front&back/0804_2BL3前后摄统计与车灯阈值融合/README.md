# 2BL3前后摄统计与车灯阈值融合实例

该实例以稀疏响应缓存版本为基线，将全图灰度统计和首次车灯二值化合并为一次顺序扫描，不修改识别公式或参数。

- 同一次读图完成灰度和、峰值最低灰度像素计数以及车灯二值图生成。
- 保留原有上下区域不同车灯阈值、顶区间隙连接和访问状态初始化顺序。
- 稀疏/稠密峰值路径、积分响应缓存和所有候选规则保持不变。
- 稠密路径、局部特征、车灯、地平线和时序逻辑保持不变。
- 算法Build ID为`0x20260919`。

在上位机中点击“加载实例”，选择本目录即可逐帧查看实际算法输出。
