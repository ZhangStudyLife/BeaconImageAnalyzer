# 2BL3前后摄局部特征必要条件短路实例

该实例基于四行响应缓存版本，只对点光源局部特征计算增加严格必要条件短路，不修改接收阈值或分类结果。

- 第一遍仍完整计算环形背景中位数、峰值、内对比度和径向衰减。
- 现有全部点光源接收分支都至少要求`径向衰减≥31`或`内对比度≥170`之一。
- 当两项同时不足时，候选在数学上不可能通过任何现有分支，因此跳过第二遍权重矩、协方差和开方。
- 非点光源的最终候选可靠性检查仍执行完整局部特征，避免改变其独立规则。
- 无新增经验参数、位置判断或视频特例。
- 算法Build ID为`0x20260939`。
