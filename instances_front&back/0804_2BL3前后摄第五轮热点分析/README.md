# 2BL3前后摄第五轮热点分析

该实例从`0x20260941`复制，仅在定义`IMAGE_HOST_PROFILE`时增加主机周期统计，用于定位结构优化后的剩余热点。

- 默认上位机加载不启用统计代码，识别规则与`0x20260941`一致。
- 统计车灯阈值化、车灯主检/补检、峰值搜索、局部特征、连通域补充和结果整理耗时。
- 不修改任何检测参数、候选顺序或时序规则。
- 算法Build ID为`0x20260943`。

该实例只用于性能分析，不作为板端移植候选。
