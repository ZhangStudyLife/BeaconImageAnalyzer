# 2BL3前后摄车灯前景位图调度实例

该实例基于局部特征必要条件短路版本，将同一前景位图调度结构扩展到车灯主检和时序补检，不修改车灯特征或选择规则。

- 普通阈值、上方阈值和断点桥接仍保持原值与原顺序。
- 二值化时同步生成逐行前景位图；桥接新增像素同时写入位图。
- 车灯主检只遍历前景置位像素，不再扫描整幅背景。
- 时序车灯补检只遍历预测矩形覆盖的前景位图字。
- 连通域仍按原行优先顺序消费，面积、形状、灰度均匀性和最终排序不变。
- 位图复用现有阶段性缓冲区，无动态内存或新增参数。
- 算法Build ID为`0x20260940`。
