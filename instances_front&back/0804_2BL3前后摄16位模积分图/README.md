# 2BL3前后摄16位模积分图实例

该实例基于积分镜像直通版本，将稀疏峰值路径的32位积分图改为16位模积分图，不修改任何检测规则或参数。

- 算法只查询3×3和9×9窗口，最大真实窗口和为`9×9×255=20655`，严格小于65536。
- 积分图按16位自然回绕保存；四角包含排除运算也按16位回绕，最终窗口和与32位积分图完全相同。
- 峰值响应、Top16顺序、局部特征和输出时序保持不变。
- 积分缓冲读写宽度减半，并显著降低共享工作区占用。
- 固定内存，无新增参数或场景分支。
- 算法Build ID为`0x20260937`。
