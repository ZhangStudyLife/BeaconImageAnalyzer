# 2BL3前后摄第四轮结构热点分析

该实例从`0x20260930`复制，仅在定义`IMAGE_HOST_PROFILE`时增加主机端周期统计，用于定位累计优化后的剩余耗时。

- 识别公式、阈值、参数、候选顺序和时序规则不变。
- 默认由上位机加载时不启用统计代码，识别行为应与`0x20260930`完全一致。
- 专用诊断DLL统计车灯、信标峰值、局部特征、连通域补充和结果整理耗时。
- 算法Build ID为`0x20260932`。

该实例只用于性能分析，不作为最终板端移植候选。
