# 2BL3前后摄两级峰值搜索验证实例

该实例用于验证复杂亮场下的通用两级峰值搜索，不包含针对视频帧或固定位置的参数。

- 暗场继续执行完全等价的逐亮像素积分响应路径。
- 复杂亮场先寻找原始灰度3×3局部峰值，再在其周围3×3区域执行原有精确盒响应和8邻域极大值判断。
- 最终候选仍使用相同Top16、地平线、车灯屏蔽、局部灰度特征和时序规则。
- 原始灰度局部峰值仅作为计算种子，不直接成为信标输出。
- 算法Build ID为`0x20260913`。

该实例允许与基线出现少量候选差异，必须通过完整视频和人工标注回归后才能考虑板端移植。
