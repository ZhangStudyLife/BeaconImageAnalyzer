# 2BL3前后摄局部特征去平方根实例

该实例在稀疏响应缓存实例基础上减少局部灰度特征中的浮点平方根，不修改候选生成或识别阈值。

- 伸长率保存为`eigen_major / eigen_minor`，与原阈值的平方比较。
- 质心偏移保存为`centroid_x² + centroid_y²`，与原距离阈值的平方比较。
- 利用平方根在非负区间单调递增的性质，所有判定在数学上与原逻辑等价。
- 每次局部特征计算减少两个`sqrtf()`调用。
- 算法Build ID为`0x20260915`。

在上位机中点击“加载实例”，选择本目录即可逐帧查看实际算法输出。
